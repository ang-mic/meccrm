package io.meccrm.framework

import org.scalatest.FlatSpec


class BootableSpec extends FlatSpec {

  "A `Bootable`" should "return `Sig(0)` when `boot` is successful" in {
  }

  "A `Bootable`" should "return `Sig(0)` when `halt` gracefully" in {}
}
